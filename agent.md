# 삼정중 학사일정 바탕화면 위젯 — 새 프로젝트 기획안

이 문서는 새 프로젝트를 시작하는 개발 에이전트가 읽는 기획·구현 기준이다. 현재 단계는 기획이며, 아래의 앱과 조회 API는 아직 구현되지 않았다. 기존 온라인 교무실과 별도 프로젝트로 개발한다.

## 1. 목적

교직원이 Windows에 로그인하면 바탕화면 한쪽에 작은 학사일정 창이 나타나고, 온라인 교무실에 등록된 오늘·다가오는 일정을 확인할 수 있게 한다. 교무실에서 일정을 수정하면 위젯에도 자동으로 반영된다.

가칭: **삼정 학사일정 위젯**

- 사용자: 삼정중학교 교직원. 개인 설치 후 사용하는 방식부터 시작한다.
- 기준 운영체제: Windows 11 x64. 다른 버전·아키텍처는 실사용 환경 확인 후 지원 범위를 정한다.
- 표시 언어: 한국어. 일정의 기준 시간대: `Asia/Seoul`.
- 첫 버전은 일정 조회에 집중하며 추가·수정은 기본 브라우저의 온라인 교무실에서 수행한다.
- 학교 전체 PC 일괄 설치와 관리형 배포는 첫 버전 범위에 포함하지 않는다.

## 2. 바탕화면 표시 요구사항

사용자가 원하는 것은 바탕화면 한쪽에 떠 있는 위젯이다. Windows 위젯 보드에 들어가는 카드가 아니다.

| 표시 방식 | 기대 동작 |
| --- | --- |
| 기본 표시 | 작은 테두리 없는 창을 바탕화면에 배치한다. 다른 업무 프로그램을 가리지 않도록 기본적으로 항상 위 옵션은 끈다. |
| 항상 위 | 사용자가 켜면 다른 일반 창 위에서도 일정을 확인할 수 있다. |
| 접기 | 날짜와 다음 일정만 표시하는 작은 상태로 줄인다. |
| 숨기기 | 위젯 창을 감추고 트레이 아이콘은 남긴다. |
| 종료 | 트레이 메뉴에서 프로그램을 완전히 종료한다. |

**구현 전 확인할 점:** 일반 창의 ‘항상 위 해제’는 바탕화면 배경 계층에 고정하는 것과 같지 않다. `Win+D`, 모든 창 최소화, Explorer 재시작 시 계속 보이는 진짜 바탕화면 고정은 별도의 Windows 동작 검증이 필요하다. 공식 창 API의 표시 순서 제어만으로 이 요구를 충족한다고 단정하지 않는다.

첫 개발 단계에서 작은 Windows 시제품으로 위 동작을 확인한다. 바탕화면 계층에 붙이는 방식이 비공식 Explorer 구조에 의존하면 호환성·복구 방식을 문서화한다. 결과를 바탕으로 기본 표시 방식을 확정하며, 단순히 항상 위 창을 만드는 것으로 바탕화면 고정 요구가 완료됐다고 보고하지 않는다.

## 3. 첫 버전 기능

### 위젯 화면

- 기본 크기 약 340 × 420 논리 픽셀. 드래그 이동, 크기 조절, 위치·크기 저장.
- 오늘 날짜·요일, 오늘 일정, 향후 7일 일정 표시.
- 하루 종일 일정과 시간이 있는 일정 구분. 날짜별로 묶고 하루 종일 항목 다음 시간순으로 정렬.
- 학사·평가·교육 및 행사·휴업 및 방학 종류를 색과 글자로 구분.
- 여러 날 이어지는 방학·상담주간은 오늘 포함 여부를 계산해 표시.
- 제목이 길면 읽을 수 있도록 줄바꿈 또는 상세 보기 제공.
- 일정이 없을 때 ‘오늘 등록된 일정이 없습니다’ 표시.
- 마지막 갱신 시각, 수동 새로고침, 교무실 학사일정 열기 제공.
- 일정 클릭 시 상세 내용을 표시하고 ‘교무실에서 보기’ 버튼으로 `/calendar`를 연다. 기존 사이트에 일정별 상세 URL은 없으므로 임의로 만들어 사용하지 않는다.
- 밝은 화면·어두운 화면 지원. 키보드 접근과 화면 배율 100~200% 고려.

### Windows 기능

- 최초 설정에서 ‘Windows 로그인 시 자동 실행’을 선택할 수 있게 한다. 설치만으로 조용히 자동 실행을 등록하지 않는다.
- 트레이 메뉴: 표시/숨기기, 새로고침, 항상 위, 자동 실행, 설정, 로그아웃, 종료.
- 중복 실행 시 기존 창을 표시하고 추가 프로세스를 종료한다.
- 모니터가 분리되면 위젯을 현재 화면 안으로 복귀시킨다.
- 자동 갱신 시 창을 앞으로 가져오거나 입력 포커스를 빼앗지 않는다.
- 설치 제거 시 자동 실행 등록도 정리한다.

### 후속 기능

월별 미니 달력, 일정 시작 전 알림, 알림 시간 설정, 글자 크기·투명도·위치 잠금, 서명된 자동 업데이트는 첫 버전 이후 검토한다. 첫 버전에 자동으로 모두 추가하지 않는다.

## 4. 기존 온라인 교무실과의 관계

기존 저장소는 `soomsci/SJOWS`이며, 웹 서비스는 Next.js·Auth.js·Neon Postgres를 사용한다. 새 프로젝트에는 데스크톱 앱과 모의 일정 데이터만 둔다. 웹 API 변경은 기존 SJOWS 저장소의 별도 작업으로 관리한다.

현재 일정은 **기본 일정과 DB 수정값의 결합**으로 표시된다. DB 테이블만 가져오면 PDF에서 옮긴 기본 일정이 빠지므로 반드시 같은 결합 규칙을 사용해야 한다.

| 기존 파일 | 역할 |
| --- | --- |
| `src/lib/school-calendar.ts` | PDF 기반 기본 일정 및 일정 타입. 기본 일정 ID는 현재 `pdf-<배열 인덱스>` 형식이다. |
| `src/lib/schema.ts` | `calendar_events` 테이블 정의. |
| `src/lib/db.ts` | 일정 수정값 조회·저장. |
| `src/components/SchoolCalendar.tsx` | 기본 일정과 수정값 결합 및 화면 표시. |
| `src/app/calendar-actions.ts` | 공용 일정 추가·수정·삭제. |
| `src/app/calendar/page.tsx` | 교직원 인증 후 달력 표시. |
| `src/middleware.ts`, `src/auth.config.ts` | 현재 웹 로그인 보호. |
| `drizzle/0004_calendar_events.sql` | 관리자 수동 적용용 일정 테이블 SQL. |

DB SQL의 실제 적용 여부는 코드만으로 확정할 수 없다. 에이전트가 운영 DB에 접속해 확인하지 말고 담당자 확인 또는 승인된 별도 검증 절차를 따른다.

결합 규칙:

1. 기본 일정을 ID로 구성한다.
2. DB에 같은 ID가 있으면 기본 일정 전체를 DB 값으로 대체한다.
3. DB에만 있는 새 일정을 추가한다.
4. `deleted = true`인 일정은 결과에서 제외한다.
5. 요청 날짜 범위와 겹치는 항목을 반환한다. 시작일뿐 아니라 종료일도 비교한다.

웹과 위젯 API가 같은 순수 함수를 쓰도록 결합 로직을 공통 모듈로 추출한다. 현재 기본 일정 ID는 배열 순서에 의존하므로 배열을 재정렬하거나 중간에 삽입해 ID를 바꾸지 않는다. 영구 ID 전환이 필요하면 기존 수정값과의 대응 계획을 먼저 만든다.

기본 일정은 PDF를 수동 변환한 자료다. 위젯 공개 전 원본 PDF와 날짜·행사명을 대조하고, 오류를 발견하면 근거와 수정 대상을 기록한다. 해당 과정은 DB 조회 없이 수행할 수 있다.

## 5. 연동 구조와 API 초안

```text
Windows 위젯
    │ HTTPS + 기기별 인증
    ▼
온라인 교무실의 읽기 전용 일정 API (신규)
    ├─ 교직원 권한 확인
    ├─ 기본 일정 + DB 수정값 결합
    └─ 요청 기간의 일정 반환
```

Windows 프로그램은 Neon에 직접 접속하지 않는다. DB 접속 문자열, 서비스 계정 키, `AUTH_SECRET`을 앱에 넣지 않는다.

제안 조회 경로는 `GET /api/widget/calendar?from=2026-09-08&to=2026-09-14`다. **현재 존재하는 API가 아니며**, 구현 시 계약을 확정한다. 최대 요청 기간은 93일로 제한하는 안을 사용한다.

응답 예시는 실제 학사일정이 아닌 설명용 데이터다.

```json
{
  "schemaVersion": 1,
  "timezone": "Asia/Seoul",
  "generatedAt": "2026-09-08T00:00:00Z",
  "events": [
    {
      "id": "example-meeting",
      "title": "예시 교직원 회의",
      "kind": "activity",
      "date": "2026-09-08",
      "endDate": null,
      "allDay": false,
      "startTime": "15:30",
      "endTime": "16:00"
    }
  ]
}
```

- `date`와 `endDate`는 `YYYY-MM-DD`; 종료일은 포함한다. 종료일이 없으면 시작일과 같다.
- 시간은 한국 기준 `HH:mm`; `allDay = true`이면 두 시간은 `null`이다.
- 여러 날에 걸치면서 시간이 있는 일정은 기존 웹의 의미가 명확하지 않다. 단일 연속 일정인지 매일 반복인지 API 작업 때 확정하고, 그 전에는 날짜 범위와 원래 시간값을 표시하되 반복·알림을 임의로 계산하지 않는다.
- 응답에는 DB 접속 정보, 교직원 명단, 작성자 이메일 등 화면에 필요 없는 정보를 넣지 않는다.
- 잘못된 요청은 400, 인증 만료는 401, 권한 없음은 403, 일정 조회 불가는 503 등으로 구분한다.
- 위젯 API는 로그인 HTML이나 리디렉션 대신 JSON 오류를 반환한다. 기존 미들웨어와 충돌하지 않도록 해당 경로의 인증 경계를 명시적으로 구성한다.
- DB 조회가 실패하면 기본 일정만을 ‘최신 전체 일정’으로 반환하지 않는다. 실패를 알리고 앱에서 이전 캐시와 지연 안내를 표시한다.

## 6. 로그인과 기기 연결

권장 흐름은 기본 브라우저의 학교 Google 로그인과 일회성 기기 연결이다. 앱 안의 내장 웹뷰에서 Google 로그인 화면을 직접 띄우는 방식은 기본안으로 사용하지 않는다.

1. 앱에서 ‘학교 계정으로 연결’을 누르면 기본 브라우저가 열린다.
2. 사용자가 기존 교무실에 로그인하고 연결할 기기를 확인한다.
3. 짧은 유효기간의 일회성 코드를 앱에 전달하고, 앱은 기기에 묶인 읽기 전용 인증으로 교환한다.
4. 앱은 이후 인증을 갱신하며 일정만 조회한다.

이 기능은 기존 Auth.js 로그인만으로 자동 제공되지 않는다. 연결 코드·재사용 방지·기기 인증 발급·갱신·철회와 관련 저장소가 별도로 필요하다. 구체적인 프로토콜은 표준 OAuth 방식과 위협 모델을 검토해 정한다. 자체 인증을 임시 고정 토큰으로 대체하지 않는다.

- 앱에 내장된 공통 비밀번호 또는 모든 교직원이 공유하는 API 키는 사용하지 않는다.
- 인증 값은 Windows가 보호하는 자격 증명 저장소에 저장한다. 프런트엔드 localStorage와 평문 설정 파일에는 저장하지 않는다.
- 교직원 권한을 서버에서 확인하고, 탈퇴·권한 회수 시 다음 서버 조회부터 접근을 차단한다.
- 로그아웃 또는 401·403 수신 시 일정 캐시를 숨기고 인증 값과 관련 캐시를 정리한다.
- 웹의 권한 검증을 유지한다. 위젯 로그인 편의를 위해 기존 페이지를 공개하지 않는다.

## 7. 갱신·캐시·오류 처리

- 기본 5분 간격 갱신. 기기별로 작은 무작위 지연을 두어 동시 요청을 분산한다.
- 시작, 절전 복귀, 네트워크 복구, 수동 새로고침 때 갱신한다. 진행 중 요청을 중복 실행하지 않는다.
- 잠든 PC에서 놓친 타이머 횟수만큼 요청하지 않고 복귀 시 한 번 갱신한다.
- 네트워크 오류에는 점진적으로 재시도 간격을 늘리고 최대 30분으로 제한한다.
- 캐시는 Windows 사용자·학교 서버·로그인 계정별로 분리한다. 민감한 내용의 로그 기록을 피한다.
- 오프라인일 때는 마지막 성공 시각과 ‘연결 끊김’ 상태를 표시한다. 캐시도 없으면 로그인/연결 안내를 표시한다.
- 오프라인 상태에서는 권한 회수를 즉시 알 수 없다는 한계가 있다. 보관 기간 기본안을 24시간으로 두고 만료 후 캐시 내용을 숨긴다.
- 자정에 한국 기준 날짜와 요청 기간을 갱신한다. PC의 현지 시간대에 따라 학사일정 날짜가 바뀌지 않아야 한다.
- 2027년 이후 일정을 처리할 수 있도록 특정 학년도 시작·종료를 앱에 고정하지 않는다.

## 8. 기술 구성 제안

기본안은 **Tauri 2 + React + TypeScript**다. 화면은 로컬 번들로 실행하고, 네이티브 기능·인증 저장·API 통신은 제한된 명령 인터페이스로 제공한다. 기존 교무실 전체 페이지를 그대로 위젯에 넣는 방식은 사용하지 않는다.

- Tauri의 자동 실행·트레이·창 위치 기능을 사용한다. 패키지 버전과 지원 범위는 개발 시작 시 공식 문서로 다시 확인한다.
- 실제 바탕화면 고정이 Tauri만으로 충족되지 않으면 Windows 전용 창 처리 계층을 분리한다.
- 브라우저에 여는 주소는 승인된 교무실 HTTPS 주소로 제한한다. 일정 데이터로부터 임의 명령을 실행하지 않는다.
- 화면 갱신은 이벤트·타이머 중심으로 처리하고 상시 애니메이션은 최소화한다.
- Windows 설치 파일은 Windows 개발 환경 또는 Windows CI에서 만든다. macOS에서 화면을 확인한 것만으로 Windows 작동 완료를 선언하지 않는다.

제안 디렉터리:

```text
school-calendar-widget/
  agent.md
  README.md
  src/
    components/       # 위젯, 일정 목록, 설정, 로그인 상태
    calendar/         # 날짜 범위, 정렬, 표시 규칙
    fixtures/         # 운영 DB가 필요 없는 예시 일정
  src-tauri/
    src/             # 창, 트레이, 자동 실행, 인증, 조회, 캐시
  docs/
    API_CONTRACT.md
    WINDOWS_BEHAVIOR.md
    INSTALLATION.md
    RELEASE_CHECKLIST.md
```

## 9. 개발 순서

1. **Windows 표시 검증**: 모의 데이터로 창을 만들고 바탕화면 표시, Win+D, 항상 위, 다중 모니터, 트레이, 자동 실행을 확인한다.
2. **화면 구현**: 오늘·향후 7일, 하루 종일·시간 지정, 긴 제목, 빈 상태, 오프라인 화면을 만든다.
3. **서버 계약 정의**: 기본 일정과 수정값의 공통 결합 함수, 조회 API, 날짜 의미, 인증 및 기기 연결 계약을 확정한다.
4. **인증과 API 구현**: 기존 SJOWS 프로젝트에서 별도로 작업한다. DB 스키마가 필요하면 SQL만 준비하고 승인된 담당자가 적용한다.
5. **앱 연동**: 인증 저장, 갱신, 캐시, 절전 복귀 및 네트워크 복구를 연결한다.
6. **Windows 시험 배포**: 담당 교직원 PC에서 설치·사용·제거를 확인하고 설치 안내와 버전을 정리한다.

API나 기기 연결 기능이 준비되지 않았더라도 모의 데이터를 사용하는 위젯 화면과 Windows 동작은 독립적으로 진행한다. 미구현 연동 기능을 동작하는 것처럼 표시하지 않는다.

## 10. 완료 기준

- 선택한 자동 실행 설정이 재로그인 후 반영되고, 앱은 한 개만 실행된다.
- 바탕화면 표시 방식과 Win+D 동작을 Windows 실기기에서 확인하고 결과를 기록한다.
- 위치와 크기가 유지되고 모니터 분리 후에도 화면 밖에 사라지지 않는다.
- 웹과 위젯에서 기본 일정·추가 일정·수정 일정·삭제 일정의 결과가 일치한다.
- 하루 종일, 지정 시간, 월·연도 경계를 넘는 일정이 올바른 날짜에 표시된다.
- 정상 연결 시 웹 변경은 기본 갱신 주기 내 반영된다.
- 인증 만료·권한 회수·오프라인을 구분해 표시한다.
- 로그아웃한 계정의 인증 값과 일정 캐시가 다음 계정에 노출되지 않는다.
- 설치 파일에 운영 비밀값이 없고, 설치 제거 시 자동 실행 항목이 남지 않는다.
- 결과 보고에 실제로 실행한 확인과 아직 확인하지 못한 Windows·운영 연동 항목을 구분한다.

## 11. 개발 에이전트 작업 규칙

- 이 문서는 새 프로젝트의 기획안이다. 문서 작성 요청 자체가 앱 개발, 운영 배포 또는 DB 접속 권한을 주지는 않는다.
- 실제 개발 요청을 받으면 먼저 이 문서를 읽고, 모의 데이터를 사용하는 로컬 구현부터 시작한다.
- 기존 SJOWS의 DB 접근 금지 지침을 유지한다. 에이전트는 운영 DB 읽기·쓰기, SQL 실행, 마이그레이션·시드·DB 관리 도구 실행을 하지 않는다.
- DB 접속 정보를 환경 파일에서 읽거나 출력하지 않는다. 개발·테스트는 순수 함수 및 모의 API로 진행한다.
- 인증 시스템 변경·운영 사이트 배포·DB 저장소 추가는 각각 해당 프로젝트의 승인 범위 안에서 수행한다. 새 프로젝트 생성만으로 기존 운영 서비스 수정까지 승인됐다고 해석하지 않는다.
- 서명되지 않은 설치 파일의 Windows 경고를 사용자에게 숨기거나 보안 기능 해제를 안내하지 않는다. 배포 단계에서 코드 서명 비용·방법을 정한다.
- 이 파일명은 사용자가 요청한 `agent.md`다. 사용하는 개발 도구가 지침을 자동으로 읽는 파일명이 다른 경우, 새 프로젝트 시작 시 해당 도구의 지침 설정에 등록한다.

## 12. 참고 문서

아래는 기술 선택의 참고 자료이며, 지원 여부는 구현 시 다시 확인한다.

- [Tauri 자동 실행](https://v2.tauri.app/plugin/autostart/)
- [Tauri 트레이](https://v2.tauri.app/learn/system-tray/)
- [Tauri Windows 설치 파일](https://v2.tauri.app/distribute/windows-installer/)
- [Microsoft SetWindowPos — 창 표시 순서](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setwindowpos)
